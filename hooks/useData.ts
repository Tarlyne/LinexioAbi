import { useDataManagement } from './useDataManagement';

/**
 * VERALTET: Bitte useDataManagement verwenden oder direkt den DataContext nutzen.
 * Dieser Export bleibt zur Abwärtskompatibilität bestehen.
 */
export const useData = useDataManagement;